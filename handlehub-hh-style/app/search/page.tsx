import { Marketplace } from "@/components/marketplace";
import { demoListings } from "@/lib/demo-data";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const term=q.trim().toLowerCase();const results=term?demoListings.filter(x=>(x.title+" "+x.categoryName+" "+x.seller+" "+x.description).toLowerCase().includes(term)):demoListings.slice(0,20);return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="mb-8"><div className="text-xs font-semibold uppercase tracking-[.22em] text-violet-400">Search</div><h1 className="mt-2 text-4xl font-black tracking-tight">{term?`Results for “${q}”`:"Search HandleHub"}</h1><p className="mt-2 text-zinc-500">{results.length} listing{results.length===1?"":"s"} found.</p></div><Marketplace listings={results}/></main>}
