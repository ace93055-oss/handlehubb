"use client";
import { useState } from "react";
import { X } from "lucide-react";
import { PurchasePanel } from "./purchase-panel";
export function ListingPurchase({listingId,price,title}:{listingId:string;price:number;title:string}){const[open,setOpen]=useState(false);return <><div className="hh-card p-5"><div className="text-xs text-gray-600">Buy now</div><div className="mt-1 text-3xl font-bold tracking-tight text-white">${price.toFixed(2)}</div><button onClick={()=>setOpen(true)} className="hh-btn hh-btn-blue mt-5 w-full">Buy now</button><p className="mt-3 text-center text-[11px] leading-5 text-gray-600">Pay with BTC, LTC, SOL or ETH. Invoice expires in 20 minutes or less.</p></div>{open&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/75 p-4 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><div className="invoice-pop relative max-h-[94vh] w-full max-w-md overflow-y-auto rounded-xl border border-line bg-[#0b0f16] p-1 shadow-2xl"><button onClick={()=>setOpen(false)} className="absolute right-3 top-3 z-10 flex h-8 w-8 items-center justify-center rounded-md bg-black/30 text-gray-500 hover:text-white"><X className="h-4 w-4"/></button><PurchasePanel listingId={listingId} price={price} title={title}/></div></div>}</>}
