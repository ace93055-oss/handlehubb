import Link from "next/link";
import { BadgeCheck, Clock3 } from "lucide-react";
import type { DemoListing } from "@/lib/demo-data";
export function ListingCard({listing}:{listing:DemoListing}){return <Link href={`/${listing.category}/${listing.slug}`} className="hh-card group block p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex items-center gap-1.5"><h3 className="truncate text-[15px] font-semibold text-gray-100 group-hover:text-white">{listing.title}</h3>{listing.verified&&<BadgeCheck className="h-3.5 w-3.5 shrink-0 text-blue-400"/>}</div><div className="mt-1 text-xs text-gray-600">{listing.categoryName}</div></div>{listing.featured&&<span className="rounded-md bg-blue-500/10 px-2 py-1 text-[10px] font-semibold uppercase tracking-wide text-blue-300">Featured</span>}</div><div className="mt-5 flex items-end justify-between gap-3"><div><div className="text-[11px] text-gray-600">Buy now</div><div className="mt-0.5 text-xl font-bold tracking-tight text-white">${listing.priceUsd.toFixed(0)}</div></div><div className="text-right"><div className="text-xs font-medium text-gray-300">@{listing.seller}</div><div className="mt-1 text-[11px] text-gray-600">★ {listing.rating.toFixed(2)} · {listing.sales} sales</div></div></div><div className="mt-3 flex items-center justify-between border-t border-line/70 pt-3 text-[11px] text-gray-600"><span className={listing.status==="AVAILABLE"?"text-emerald-400":"text-amber-400"}>{listing.status==="AVAILABLE"?"Available":"Reserved"}</span><span className="flex items-center gap-1"><Clock3 className="h-3 w-3"/>{new Date(listing.createdAt).toLocaleDateString(undefined,{month:"short",day:"numeric"})}</span></div></Link>}
