import { Flame, ShoppingCart } from "lucide-react";
import { CategoryStrip } from "./category-strip";
import { Marketplace } from "./marketplace";
import type { DemoListing } from "@/lib/demo-data";
export function PageShell({title,active,listings,trending=false}:{title:string;active?:string;listings:DemoListing[];trending?:boolean}){return <main className="mx-auto w-full max-w-none px-4 pb-20 pt-0 desktop:px-6 desktop:pb-6"><CategoryStrip active={active}/><div className="mb-3 flex gap-2 overflow-x-auto py-0.5 hh-scrollbar"><span className="hh-pill hh-pill-active"><ShoppingCart className="h-3.5 w-3.5"/>Buy Now <span className="rounded bg-blue-500/15 px-1.5 py-0.5 font-mono text-[10px]">{listings.length}</span></span><span className={`hh-pill ${trending?"hh-pill-active":""}`}><Flame className="h-3.5 w-3.5"/>Trending</span><span className="hh-pill">Telegram 4L</span><span className="hh-pill">Instagram 4L</span><span className="hh-pill">Minecraft Capes</span><span className="hh-pill">Roblox Limiteds</span></div><div className="mb-3 grid grid-cols-2 gap-3 md:grid-cols-3"><div className="hh-card p-3.5"><div className="text-[11px] uppercase tracking-wide text-gray-600">Marketplace</div><div className="mt-1 text-sm font-semibold text-gray-200">{title}</div></div><div className="hh-card p-3.5"><div className="text-[11px] uppercase tracking-wide text-gray-600">Available</div><div className="mt-1 text-sm font-semibold text-gray-200">{listings.filter(x=>x.status==="AVAILABLE").length} active</div></div><div className="hh-card hidden p-3.5 md:block"><div className="text-[11px] uppercase tracking-wide text-gray-600">Protected trades</div><div className="mt-1 text-sm font-semibold text-gray-200">Escrow available</div></div></div><Marketplace listings={listings}/></main>}
